ANGULAR AMPLITUDEJS

AngularJS wrapper for the Amplitude SDK.

BEFORE THE INSTALL:
Check that you have npm, bower, and typings installed:

$ npm --version
$ bower --version
$ typings --version

TO INSTALL:
$ npm install
